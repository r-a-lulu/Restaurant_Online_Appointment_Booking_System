## Database Archive

This folder keeps SQL objects that are not part of the current core app flow.

The root `database/` files remain the active runnable set.

Archive contents:
- `procedures_optional.sql`
- `functions_optional.sql`
- `views_optional.sql`
- `events_optional.sql`
- `triggers_audit.sql`

These files are for reference, cleanup review, or future restoration.
