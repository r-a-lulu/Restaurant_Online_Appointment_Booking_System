# Stored Procedures Support Note

This file is documentation only.

The current runnable procedure set lives in:
- `database/procedures.sql`

The current import order lives in:
- `database/README.md`

Important:
- The root `database/` files are the source of truth for the active project.
- Archived or optional procedures were moved to `database/archive/`.
- Older procedure counts and numbering in past notes are no longer authoritative.
