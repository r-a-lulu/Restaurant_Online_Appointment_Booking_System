# Database Views Support Note

This file is documentation only.

The current runnable view set lives in:
- `database/views.sql`

The current import order lives in:
- `database/README.md`

Important:
- The root `database/` files are the source of truth for the active project.
- Archived or optional views were moved to `database/archive/`.
- Older notes that mention archived views such as `vw_available_tables` are no longer authoritative.
