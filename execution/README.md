# Execution Scripts

This directory contains Layer 3 logic (Doing the work).
These are deterministic Python scripts acting as reliable, testable, fast tools.
DO NOT use manual work; execute Python scripts built here.
